# P8A scope boundary

This branch inventories current public routes, code/data owners, provider bindings, controls, states, existing acceptance evidence, and explicit gaps.

It does not repair product UI, add public pages, change APIs, alter D1, modify collectors, change retention, or deploy a Preview.

Exact next branch after merge reporting and explicit continuation:

```text
work-public-browser-audit
```
