# Heatmap PR 2 Scope

This branch establishes the page boundary and lifecycle layer only. It intentionally preserves current visual and data behavior through the temporary legacy adapter. Later PRs replace the adapter one boundary at a time.
