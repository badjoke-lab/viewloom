# Heatmap PR 10

PR 10 completes the snapshot summary, visual legend, coverage note, and visible automatic-refresh state.

## Summary

The page shows active observed records, total observed viewers, strongest momentum, and highest available sampled activity. Unavailable, not sampled, and sampled zero activity remain distinct.

## Legend

The legend states that area is viewers, green is rising, red is falling, blue-gray is stable, and activity is a secondary availability-dependent accent.

## Coverage

The page separates observed records, configured collection limit, has-more state, covered pages, source mode, collection method, snapshot age, and normalized warnings.

## Refresh state

Automatic refresh remains on at the existing sixty-second cadence. The Control Dock shows request progress, the next stored-snapshot check, failure and retry state, and hidden-tab waiting behavior. Refresh reads stored data and does not claim to force a collector run.

PR 11 remains mobile and accessibility work. PR 12 remains cross-platform QA, legacy cleanup, and production cutover.
