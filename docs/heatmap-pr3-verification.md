# Heatmap PR 3 Verification

Required checks are web typecheck, web build, Heatmap boundary verification, and Heatmap data-state fixture verification. This PR is not ready to merge until those checks pass.
