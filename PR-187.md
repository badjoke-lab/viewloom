# PR-187

## Summary

Make the Python Kick live probe the standard operator command.

## Changes

- `kick:live-probe` now uses the Python probe.
- `kick:live-probe:safe` and `kick:live-probe:slow` use Python variants.
- Node probe commands remain available under `kick:live-probe:node*`.
- README updated.

## Scope

Package script and docs only.
