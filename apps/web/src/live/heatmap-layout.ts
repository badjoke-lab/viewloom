export function initHeatmapLayout(): void {
  // Compatibility entry for the retired single-page shell in src/main.ts.
  // Public Heatmap routes install layout behavior through heatmap-page/layout-mode.ts.
}
