name: Reconcile category parallel execution gate

on:
  push:
    branches:
      - agent/category-parallel-execution-docs
    paths:
      - '.github/workflows/reconcile-12a4-category-parallel-execution.yml'
      - 'scripts/reconcile-12a4-category-parallel-execution.mjs'

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/category-parallel-execution-docs
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Advance canonical category gate
        run: node scripts/reconcile-12a4-category-parallel-execution.mjs
      - name: Commit reconciled gate
        run: |
          if git diff --quiet; then
            echo 'No reconciliation changes.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/audits/12a2-current-gate-state.json
          git commit -m 'Advance category gate to parallel execution'
          git push origin HEAD:agent/category-parallel-execution-docs