name: History Battle Archive

on:
  workflow_dispatch:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.17.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm --filter @viewloom/web typecheck
      - run: node scripts/verify-history-streamer-daily-stats.mjs
        working-directory: apps/web
      - run: node scripts/verify-history-additional-rankings.mjs
        working-directory: apps/web
      - run: node scripts/verify-history-peak-archive.mjs
        working-directory: apps/web
      - run: node scripts/verify-history-battle-archive.mjs
        working-directory: apps/web
      - run: node scripts/verify-deep-link-contract.mjs
        working-directory: apps/web
      - run: pnpm build:web
