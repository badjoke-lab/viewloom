name: Watchlist Storage

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'apps/web/src/live/watchlist/**'
      - 'apps/web/scripts/verify-watchlist-storage.mjs'
      - 'docs/product/local-watchlist-spec.md'
      - 'docs/product/watchlist-v1-implementation-plan.md'
      - 'docs/product/current-roadmap.md'
      - 'docs/product/current-schedule.md'
      - 'docs/work-in-progress/watchlist-v1-working-note.md'
      - '.github/workflows/watchlist-storage.yml'
  push:
    branches: [main]
    paths:
      - 'apps/web/src/live/watchlist/**'
      - 'apps/web/scripts/verify-watchlist-storage.mjs'
      - 'docs/product/local-watchlist-spec.md'
      - 'docs/product/watchlist-v1-implementation-plan.md'
      - 'docs/product/current-roadmap.md'
      - 'docs/product/current-schedule.md'
      - 'docs/work-in-progress/watchlist-v1-working-note.md'
      - '.github/workflows/watchlist-storage.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.17.1
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile
      - name: Typecheck web application
        run: pnpm --filter @viewloom/web typecheck:app
      - name: Verify Watchlist storage contract
        shell: bash
        working-directory: apps/web
        run: |
          set +e
          node scripts/verify-watchlist-storage.mjs > watchlist-storage.log 2>&1
          status=$?
          cat watchlist-storage.log
          exit $status
      - name: Upload Watchlist storage diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: watchlist-storage-diagnostics
          path: apps/web/watchlist-storage.log
