name: History View Shell

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'apps/web/docs/history-view-shell-contract.md'
      - 'apps/web/src/history-view-shell.css'
      - 'apps/web/src/live/history-view-shell.ts'
      - 'apps/web/src/live/history-usability-pass.ts'
      - 'apps/web/scripts/verify-history-view-shell.mjs'
      - '.github/workflows/history-view-shell.yml'
      - 'scripts/verify-development-policy.mjs'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.17.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: pnpm install --no-frozen-lockfile
      - name: Typecheck History view shell
        run: pnpm --filter @viewloom/web typecheck
      - name: Verify History view shell contract
        working-directory: apps/web
        run: node scripts/verify-history-view-shell.mjs
      - run: pnpm build:web
