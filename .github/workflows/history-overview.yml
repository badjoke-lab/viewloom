name: History Overview

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'apps/web/src/history-overview.css'
      - 'apps/web/src/live/history-overview.ts'
      - 'apps/web/src/live/history-usability-pass.ts'
      - 'apps/web/scripts/verify-history-overview.mjs'
      - '.github/workflows/history-overview.yml'
      - 'scripts/verify-development-policy.mjs'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.17.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm --filter @viewloom/web typecheck
      - name: Verify History Overview
        working-directory: apps/web
        run: node scripts/verify-history-overview.mjs
      - run: pnpm build:web
